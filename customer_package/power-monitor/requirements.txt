# Core Dependencies
mysql-connector-python>=8.0.0
python-dotenv>=1.0.0
matplotlib>=3.5.0

# Email Notifications (optional)
# No extra packages needed (smtplib is built-in)

# Raspberry Pi GPIO (only needed on Raspberry Pi)
# RPi.GPIO>=0.7.1

# Note: Tkinter is part of Python's standard library but may require system packages:
# - Ubuntu/Debian: sudo apt-get install python3-tk
# - macOS: brew install python-tk
# - Raspberry Pi OS: Usually pre-installed with Python
# - Windows: Usually included with Python installation

